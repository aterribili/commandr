{
  "name": "commandr",
  "version": "0.0.1",
  "description": "A CLI to automatize my repetitive tasks",
  "main": "index.js",
  "scripts": {
    "install": "npm install -g",
    "build": "rm -rf lib/ && babel src -d lib && cp src/rn/component lib/rn && cp src/rn/test-component lib/rn",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Abner",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.9.0",
    "node-cmd": "^2.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-env": "^1.3.3"
  },
  "bin": {
    "abner": "lib/abner.js"
  }
}
